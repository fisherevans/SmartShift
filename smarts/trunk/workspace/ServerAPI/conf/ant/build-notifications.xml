<project name="Notifications" default="tomcat-deploy" basedir="../..">
	<property file="conf/ant/build.properties" />
	<property name="war.file" value="${project.name.notifications}.war" />
	<property name="war.path.notifications" value="${dist.dir}/${war.file}" />

	<path id="classpath">
		<fileset dir="${lib.dir}/jar">
			<include name="*.jar" />
		</fileset>
	</path>

	<taskdef name="deploy" classname="org.apache.catalina.ant.DeployTask">
		<classpath refid="classpath" />
	</taskdef>
	<taskdef name="undeploy" classname="org.apache.catalina.ant.UndeployTask">
		<classpath refid="classpath" />
	</taskdef>
	<taskdef name="reload" classname="org.apache.catalina.ant.ReloadTask">
		<classpath refid="classpath" />
	</taskdef>

	<target name="build-clean">
		<echo>Cleaning the build directory</echo>
		<delete dir="${out.dir}" />
	</target>
	<target name="build-init">
		<echo>Creating sourc folders</echo>
		<mkdir dir="${build.dir.notifications}/WEB-INF/" />
		<mkdir dir="${build.dir.notifications}/WEB-INF/classes"/>
		<mkdir dir="${build.dir.notifications}/WEB-INF/lib"/>
		<mkdir dir="${build.dir.notifications}/META-INF/" />
	</target>
	<target name="build-compile" depends="build-init">
		<echo>Compiling the source files</echo>
		<javac destdir="${build.dir.notifications}/WEB-INF/classes" verbose="false" debug="${build.debug}" debuglevel="lines,vars,source">
            <src path="${src.dir.api}" />  
            <src path="${src.dir.notifications}" />  
			<classpath refid="classpath" />
		</javac>
	</target>
	<target name="build-package" depends="build-compile">
		<echo>Copying compiled sources</echo>
		<copy todir="${build.dir.notifications}/WEB-INF/classes">
			<fileset dir="${conf.dir}/hibernate" />
		</copy>
		<copy todir="${build.dir.notifications}/WEB-INF/classes">
			<fileset dir="${conf.dir}/log4j" />
		</copy>
		<copy todir="${build.dir.notifications}/WEB-INF">
			<fileset dir="${conf.dir}/web/notifications" />
		</copy>
		<copy todir="${build.dir.notifications}">
			<fileset dir="${web.dir}" />
		</copy>
		<copy todir="${build.dir.notifications}/META-INF" file="${conf.dir}/web/notifications/context.xml" />
		<copy todir="${build.dir.notifications}/WEB-INF/lib">
			<fileset dir="${lib.dir}/jar">
				<exclude name="javax.servlet.jar" />
				<exclude name="servlet-api.jar" />
			</fileset>
		</copy>
	</target>
	<target name="build-war" depends="build-package">
		<echo>Building the war file</echo>
		<war destfile="${war.path.notifications}" webxml="${build.dir.notifications}/WEB-INF/web.xml">
			<fileset dir="${build.dir.notifications}" />
		</war>
	</target>

	<target name="tomcat-deploy" depends="build-war, tomcat-remove">
		<echo>Deploying war to tomcat</echo>
		<property name="war.abs.path" location="${war.path.notifications}" />
		<deploy url="${tomcat.url}"
			username="${tomcat.user}"
			password="${tomcat.pass}"
			path="/${context.root.notifications}"
			war="${war.abs.path}" />
	</target>
	<target name="tomcat-reload">
		<reload url="${tomcat.url}"
			username="${tomcat.user}"
			password="${tomcat.pass}"
			path="/${context.root.notifications}" />
	</target>
	<target name="tomcat-remove">
		<undeploy failonerror="false"
		    url="${tomcat.url}"
			username="${tomcat.user}"
			password="${tomcat.pass}"
			path="/${context.root.notifications}" />
	</target>
</project>